# AI 作業ルール

このリポジトリの Python 実行環境は、ローカル仮想環境 `.venv/` を正とします。

## Python 実行ルール

- Python コマンドは常に `.venv/bin/python` で実行します。
- `python`、`pytest` などがグローバルに入っている前提で判断しません。
- テスト実行は常に `.venv/bin/python -m pytest` を優先します。
- `PATH` 上でツールが見つからない場合も、すぐに「実行できない」と判断せず、まず `.venv/bin/python -m ...` で再実行を試みます。

## 検証ルール

- Python コードを変更したタスクを完了する前に、関連テストを `.venv/bin/python -m pytest` で実行します。
- `web_wasm/` 配下の JavaScript を変更したタスクを完了する前に、必要な `node --test ...` を実行し、該当する Python テストもあればあわせて実行します。
- 必要なテストを実行できない場合は、試したコマンドと実行できなかった具体的な理由を明記します。

## プロジェクト上の注意

- 永続化データの構造を変更する際は、既存セーブデータとの互換性を必ず意識します。
- 永続フィールドを追加する際は、スキーマ検証や runtime invariant を迂回しません。
- Wasm 側の変更時は、`web_wasm/ARCHITECTURE.md` にある詳細な設計ルールと実装ルールに従います。
